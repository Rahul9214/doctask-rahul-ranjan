name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:0.8.1-pg17-bookworm
        env:
          POSTGRES_DB: project_assurance
          POSTGRES_PASSWORD: ci_only
          POSTGRES_USER: project_assurance
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U project_assurance -d project_assurance"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    defaults:
      run:
        working-directory: backend
    env:
      DATABASE_URL: postgresql+asyncpg://project_assurance:ci_only@localhost:5432/project_assurance
      TEST_DATABASE_URL: postgresql+asyncpg://project_assurance:ci_only@localhost:5432/project_assurance_test
      ALLOW_DESTRUCTIVE_TEST_DATABASE: "true"
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          version: "0.11.26"
          enable-cache: true
      - run: uv python install 3.13.14
      - run: uv sync --frozen --all-groups
      - run: uv run python scripts/ensure_test_database.py
      - run: uv run alembic upgrade head
      - name: Verify Phase 10 test-database schema round trip
        run: |
          DATABASE_URL="$TEST_DATABASE_URL" uv run alembic upgrade head
          DATABASE_URL="$TEST_DATABASE_URL" uv run alembic downgrade 20260819_0007
          DATABASE_URL="$TEST_DATABASE_URL" uv run alembic upgrade head
          DATABASE_URL="$TEST_DATABASE_URL" uv run alembic current
      - run: uv run ruff format --check .
      - run: uv run ruff check .
      - run: uv run mypy src tests
      - run: uv run pytest --cov=app --cov-report=term-missing
      - name: Secret-pattern regression over tracked source
        run: uv run pytest tests/test_secret_scan.py tests/test_dependency_review.py -v --no-cov
      - name: Process-kill resume evidence
        run: uv run pytest tests/test_process_kill_resume.py -v --no-cov
      - name: MCP stdio server/client evidence
        run: uv run pytest tests/test_mcp_e2e.py -v --no-cov
      - name: Final register publication evidence
        run: uv run pytest tests/test_publication_api.py tests/test_publication_integration.py tests/test_publication_integrity.py tests/test_publication_concurrency.py tests/test_final_acceptance.py tests/test_publication_migration.py tests/test_ruleset.py -v --no-cov
      - run: uv build

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.20.0
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run format:check
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
